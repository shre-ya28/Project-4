pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0
streamlit>=1.32.0
joblib>=1.3.0
jupyter>=1.0.0

# Optional -- the pipeline auto-detects and uses these if installed,
# and degrades gracefully (with an explanatory note) if they are not.
# xgboost>=2.0.0
# shap>=0.44.0
